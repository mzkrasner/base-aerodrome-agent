# ===========================================
# Aerodrome Trading Agent Configuration
# ===========================================

# -----------------------------
# REQUIRED
# -----------------------------

# PostgreSQL database connection string
# Example: postgresql://user:password@localhost:5432/aerodrome_agent
DATABASE_URL=

# -----------------------------
# LLM PROVIDER CONFIGURATION
# -----------------------------

# LLM provider to use: anthropic | openai | eigenai
# Default: anthropic
LLM_PROVIDER=

# Override the default model for the selected provider (optional)
# LLM_MODEL=

# --- Anthropic (default) ---
# Get one at: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# --- OpenAI ---
# Get one at: https://platform.openai.com/
# OPENAI_API_KEY=

# --- EigenAI ---
# Two authentication methods available:
#
# Option 1: API Key (simpler, recommended for getting started)
# Uses endpoint: https://eigenai.eigencloud.xyz
# EIGENAI_API_KEY=your-api-key
#
# Option 2: Wallet Signing (verifiable inference)
# Uses endpoint: https://determinal-api.eigenarcade.com
# Requires a wallet with an active grant from EigenAI
# EIGENAI_PRIVATE_KEY=0x...
#
# If both are set, API key takes precedence.
#
# Optional EigenAI configuration:
# EIGENAI_API_URL=  # Override the default URL (not usually needed)
# EIGENAI_MODEL_ID=gpt-oss-120b-f16
#
# Available EigenAI models:
# - gpt-oss-120b-f16 (default) - 120B parameter model, tool-calling only

# -----------------------------
# RECALL API (EigenAI Verification)
# -----------------------------
# Required when using EigenAI with wallet signing for verifiable inference.
# Submits verified signatures to Recall every 15 minutes for badge status.

# Recall API base URL
# RECALL_API_URL=https://api.staging.competitions.recall.network

# Agent API key for Recall authentication
# RECALL_API_KEY=

# Competition UUID the agent is participating in
# RECALL_COMPETITION_ID=

# -----------------------------
# TRADING EXECUTION
# -----------------------------

# Private key for the TRADING wallet (with 0x prefix)
# WARNING: This wallet will execute real trades!
# Without this, the agent runs in read-only mode
AGENT_PRIVATE_KEY=

# Base chain RPC URL
# Default: https://mainnet.base.org (public, heavily rate-limited)
#
# RECOMMENDED: Use Alchemy for reliable RPC access
# 1. Create account at https://dashboard.alchemy.com
# 2. Create new app → Select "Base" chain, "Mainnet" network
# 3. Copy your API key
# 4. Set: BASE_RPC_URL=https://base-mainnet.g.alchemy.com/v2/YOUR_API_KEY
#
BASE_RPC_URL=

# -----------------------------
# TRADING PAIRS CONFIGURATION
# -----------------------------
# Specify which token pairs the agent should trade.
# Format: QUOTE/BASE pairs, comma-separated
#
# Default (if not set): WETH/USDC,AERO/USDC,WETH/AERO,cbETH/USDC,cbBTC/USDC,WBTC/USDC,VIRTUAL/WETH,BRETT/WETH,DEGEN/WETH,TOSHI/WETH
#
# Examples:
#   TRADING_PAIRS=WETH/USDC,AERO/USDC              # Just ETH and AERO
#   TRADING_PAIRS=BRETT/WETH,MIGGLES/WETH          # Meme coins only
#   TRADING_PAIRS=WBTC/USDC,EIGEN/WETH,PONKE/WETH  # Competition tokens
#
# Supported tokens: WETH, USDC, USDbC, DAI, AERO, cbETH, cbBTC, WBTC,
#                   VIRTUAL, EIGEN, BRETT, DEGEN, TOSHI, MIGGLES, PONKE
#
# TRADING_PAIRS=WETH/USDC,AERO/USDC,BRETT/WETH

# -----------------------------
# SAFETY FLAGS
# -----------------------------

# Enable dry run mode (simulates trades without execution)
# DRY_RUN=true

# Enable test mode (uses test values)
# TEST_MODE=true

# -----------------------------
# ENHANCED DATA (Optional)
# -----------------------------

# Grok API key for X/Twitter sentiment analysis
# Get one at: https://console.x.ai/
# Without this, sentiment tool will be disabled
GROK_API_KEY=

# Coingecko API key for technical analysis
COINGECKO_API_KEY=

# -----------------------------
# APPLICATION SETTINGS
# -----------------------------

# Environment: development | production
NODE_ENV=development

# Trading loop interval in minutes (default: 5)
TRADING_INTERVAL_MINUTES=5

# Maximum steps per agent iteration (default: 10)
MAX_AGENT_STEPS=10
