/**
 * Mastra Entry Point
 *
 * This is the canonical export that `mastra dev` looks for.
 * Re-exports the configured Mastra instance with all agents and tools.
 */
export { mastra } from './config.js'
